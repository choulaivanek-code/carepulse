import React from 'react';
import { PatientMessagerie } from '../patient/PatientMessagerie';

// Reuse the complex messaging structure with slightly different labels
export const AgentMessagerie: React.FC = () => {
  return <PatientMessagerie />;
};
